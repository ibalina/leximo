   </br><tr class="<%= cycle('odd', 'even') %>">
     <td><%=h user.login %></td>
     <td><%=h user.email %></td>
     <td><%= user.enabled ? 'yes' : 'no' %>
       <% unless user == current_user %>
         <% if user.enabled %>
           <%= link_to('disable', user_path(user.login), :method => :delete) %>
         <% else %>
           <%= link_to('enable', enable_user_path(user.login), :method => :put) %>
         <% end %>
  <% end %>
  </td>
     <td>[<%= link_to 'edit roles', user_roles_path(user) %>]</td>
   </tr>